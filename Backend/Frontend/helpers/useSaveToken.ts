/* eslint-disable */

import React from 'react'

const useSaveToken = () => {
    const postFbToken = async (accesstoken:string) =>{
        const resp = console.log("fire")
    }
    return {postFbToken}
}

export default useSaveToken