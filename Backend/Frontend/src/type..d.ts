export type PaymentDetails = {
  status: boolean;
  payId: string;
  company: string;
  amount: number;
  email: string;
  method: string;
  orderId: string;
  invoiceId: string;
  customerId: string;
  createdAt: string;
};
